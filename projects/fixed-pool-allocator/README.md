# Description 

Implement a Fixed-Size Memory Pool Allocator:
Design and implement a fixed-size memory pool that pre-allocates a large chunk of memory and serves fixed-size object allocations in O(1) time. The allocator must avoid fragmentation and support deallocation. 

**Status:** Planned --> to be implemented.
